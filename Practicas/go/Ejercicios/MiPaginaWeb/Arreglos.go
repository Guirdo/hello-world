// Arreglos
package main

import (
	."fmt"
)

func main() {
	make nombres := ([]string,0,10)
	make sexos := ([]string,0,10)
	make edades := ([]int,0,10)
	var nombre,sexo,respuesta string
	edad,i := 0
	var continuar bool
	
	for continuar == "Si"{
		Printf("Ingrese el nombre #%d",i)
		
		}
}
