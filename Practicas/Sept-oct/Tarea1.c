#include <cstdlib>
#include <stdio.h>
#define MAX_STRLEN 256

int int main(int argc, char const *argv[]) {
  int opc,i,num;
  char tipo [MAX_STRLEN];
  float raiz,cua,seno;

  printf ("1 - Empezar 2- Salir \n");
  printf ("R= ");
  scanf(%d,opc);

  switch(opc){

	  case 1:

    for ( i = 1; i <= 10; i++) {
      num = rand()%40;

  		if (num % 2 == 0)
  			tipo[]=par;
  		else
  			tipo[]=impar;

      cua = 0;
      raiz = 0;
      seno = 0;

      printf("%d\t%s\t%f\t%f\t%f\t",i& );

  		num = 0;
    }
	  break;

	}


  return 0;
}
